// Public browser configuration ONLY. Never put a service_role or secret key here.
// Once a separate Supabase project has been approved and provisioned, insert its
// project URL and PUBLISHABLE key. These two values are safe to expose in a browser
// only because all private tables have restrictive row-level security.
window.TRIP_PLANNER_CONFIG = { url: "", publishableKey: "" };
